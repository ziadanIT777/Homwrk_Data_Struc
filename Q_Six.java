import org.w3c.dom.Node;

public class Q_Six {
    public void circularVersion(){
        oneSentin();
        Node<E> e = trailer.getNext();
        trailer = trailer.getprev();
        trailer.setNext(e);
    }
}
